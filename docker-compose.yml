services:
  documentdb:
    image: ghcr.io/documentdb/documentdb/documentdb-local:latest
    container_name: dnd-documentdb
    command: ["--username", "admin", "--password", "dungeons123!"]
    ports:
      - "10260:10260"
    volumes:
      - documentdb_data:/data/db
    restart: unless-stopped

volumes:
  documentdb_data:
